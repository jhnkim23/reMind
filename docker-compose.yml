services:
  api:
    build: ./backend/api        
    ports:
      - "8000:8000"
    volumes:
      - ./api:/api
    command: bash -c "python manage.py runserver 0.0.0.0:8000"

  web:
    build: ./frontend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/frontend